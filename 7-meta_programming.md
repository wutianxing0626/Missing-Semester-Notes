# 当我们使用`make build`的时候, 我们在干什么

此事在OS Lab中已有记载

## 什么是make
简单来说, make是一种自动化构建东西的操作

构建的核心是三项: 
- 目标 Target: 你最终需要的东西
- 依赖 Prerequisites: 你达到目标需要的源代码/中间文件都是什么
- 规则: 有了依赖以后需要什么操作可以达到这个目标, 定义在makefile中:
```shell
<target>: <prerequisite1> <prerequisite2> ...
	<cmd>

paper.pdf: paper.tex plot-data.png
	pdflatex paper.tex

plot-%.png: %.dat plot.py # '%'作为通配符, 且本身也是变量, 可以被复用
	./plot.py -i $*.dat -o $@ # '$*'即表示匹配的通配符部分, 而'$@'表示目标文件

.PHONY: clean 
clean: 
    rm *.pdf *.aux *.log *.png 
    git ls-files -o | xargs rm -f
```

注意: 如果你的目标并非产生新文件, 例如`make clean`, 需要使用.phony作为替代, 否则如果文件夹内出现了clean文件代码无法正常工作/clean本身的代码在没有该文件的情况下会在每一次调用时均被执行

看着有点像做菜(大雾)

make语言也是一门独立的编程语言, 和shell一样显得相对奇怪一点

make的核心在于尽可能的自动化, 且尝试以最小的工作量达成目标, 因此只要依赖项不更新make就会跳过这一段, 不进行重复操作

# 版本号

版本号非常重要, 但有时候我不知道版本差异的具体影响, 以及版本号是否拥有特殊含义。

目前相对常用的一个标准是[语义版本号](https://semver.org/lang/zh-CN/), python版本也使用这一方法:

版本格式: 主版本号.次版本号.修订号

版本号递增规则如下: 

- 主版本号: 当你做了不兼容的 API 修改, 也即这个改动无法向以前的版本兼容
- 次版本号: 当你做了向下兼容的功能性新增, 例如添加了API, 且可以兼容以前的版本
- 修订号: 当你做了向下兼容的问题修正, 例如未增加API而只是修复了一部分问题

如此一来一个项目的依赖的版本只要满足主版本号没变+次版本号与修订号不低于原版本即可

eg 构建时的仓库版本如果是1.3.7则大概率也可以用1.x.y($x \geq 3 \And y \geq 7$)来构建

# 持续集成(Continuous integration): 

指在代码发生变化时, 自动根据预先设定的规则执行一些操作, 比如进行一次测试, 有助于维护大的项目

测试方法和测试术语: 

- 测试套件(Test suite): 所有测试的统称
- 单元测试(Unit test): 一种“微型测试”, 用于对某个封装的特性进行测试
- 集成测试(Integration test): 一种“宏观测试”, 针对系统的某一大部分进行, 测试其不同的特性或组件是否能 协同 工作
- 回归测试(Regression test): 一种实现特定模式的测试, 用于保证之前引起问题的 bug 不会再次出现
- 模拟(Mocking): 使用一个假的实现来替换函数、模块或类型, 屏蔽那些和测试不相关的内容. 例如“模拟网络连接” 或 “模拟硬盘”