总之是大杂烩

# 按键映射

利用软件截获键盘发出的按键事件(keypress event)并使用另外一个事件取代, 类似于将终端内的别名设置应用到键盘上

# daemon 守护进程

一系列在后台保持运行, 不需要用户手动运行或者交互的进程, 一般以 d 结尾, 例如sshd, systemd, docker daemon

systemd支持自行配置以决定配置和启用新的守护进程或系统服务

# FUSE

File syetem in User SpacE, 即在用户空间的文件系统

具体来说是允许运行在用户态上的程序实现文件系统直接调用(原本仅有内核态能够进行文件系统的调用), 
而FUSE用来将这些调用和内核联系, 在保证用户态和内核态隔离的同时有助于开发用户态程序

# 备份

一些误区: 
- 不要将数据复制存储在同一个磁盘上, 将数据备份到不同的地点存储
- 同步方案不是备份, 因为如果文件出现损坏, 这些损坏同样会污染同步文件

有效的备份方案一般包含这几个特征: 
- 版本控制: 保证数据恢复
- 仅备份增减量而非像git一样备份整个文件, 减少数据量
- 安全性: 保证不被攻击
- 经常检查
- 云端数据同样需要做离线备份

# API

Application Programming Interface

大多数线上服务提供的 API(应用程序接口)让用户可以通过编程方式来访问这些服务的数据

一般是发送一个GET请求(例如使用`curl`指令), 解析返回的json

有的API需要一些身份验证, 例如OAuth向用户提供一些私密的token(注意这玩意和你的私钥数据重要程度相似)

IFTTT(If This Then That)可以将很多 API 整合在一起, 让某 API 发生的特定事件触发在其他 API 上执行的任务

# 命令行参数
在命令行执行程序是有一些相对泛用的参数: 
- `-h`或`--help`: 获取程序的简略用法
- `-V`或`--version`: 获取程序版本
- `-v`或`--verbose`: 输出详细的运行信息, 支持多次使用(例如`-vvvvv`)以输出更详细的信息
- `--quiet`: 与上一条指令相对, 抑制除错误提示之外的其他输出
- dry run: 会造成不可撤回操作的工具一般会提供"空运行"标志参数, 这样用户可以确认工具真实运行时会进行的操作
- interactive mode: 与上一条指令相似, 在执行每个不可撤回的操作前提示用户确认
- `-r`: 会造成破坏性结果的工具一般默认进行非递归的操作, 需要显式指定是否递归
- `--`: 让程序停止处理后面出现的标志参数, 例如`rm -- -r` 会将 `-r` 当作文件名
- `-`: 一般在命令行中标志程序不从文件读入输入而是走标准输入流, 一般的程序也会支持标准输入流作为输入

# 窗口管理器
Windows的窗口管理实质上为堆叠式管理

平铺式管理: 将所有窗口均处于桌面(类似tmux), 使用键盘切换、缩放、以及移动窗口

# VPN

Virtual Private Network

不可信的VPN供应商有可能破坏你的网络数据私密性

# Markdown

不写了

# 桌面自动化

由于面向MacOS用户而惨遭跳过

# Booting and Live USB

众所周知系统启动的时候会有一段先序的硬件系统进行初始化(booting)流程

在此期间我们可以从硬盘以外的其他设备加载操作系统, 例如从USB启动

Live USB 是包含了完整操作系统的闪存盘, 用途包括但不限于: 
- 作为安装操作系统的启动盘
- 在不将操作系统安装到硬盘的情况下, 直接运行 Live USB 上的操作系统
- 对硬盘上的相同操作系统进行修复
- 恢复硬盘上的数据

Live USB 通过在闪存盘上写入操作系统的镜像制作而非直接拷贝对应文件

# Docker, Vagrant, VMs, Cloud

虚拟机/容器的用途都是创建一个隔离的包括操作系统的完整计算机系统, 
可以用于创建独立的测试或者开发环境, 以及用作安全测试的沙盒

Docker, Vagrant都是构建和配置虚拟开发环境的工具, 但docker基于容器这个更为概括的概念, 容器避免运行其他内核实例，而是与主机分享内核, 某种意义上来说是牺牲了隔离性换取了一些性能

租用云端虚拟机可以享受以下资源的即时访问: 

- 便宜、常开、且有公共 IP 地址的虚拟机用来托管网站等服务
- 有大量 CPU、磁盘、内存、以及 GPU 资源的虚拟机
- 超出用户可以使用的物理主机数量的虚拟机
- 相比物理主机的固定开支, 虚拟机的开支一般按运行的时间计算, 在某些场景下(例如在短时间内使用高并发的大量算力), 租用 1000 台虚拟机运行几分钟明显更加划算

# 交互式记事本编程
省流: Jupyter

# Github

最受欢迎的开源软件开发平台之一

我提的[一个issue](https://github.com/SJTU-IPADS/OS-Course-Lab/issues/74)和[对应的pr](https://github.com/SJTU-IPADS/OS-Course-Lab/pull/75)