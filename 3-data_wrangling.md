鉴于我更喜欢GUI界面, vim这一章惨遭跳过

这一讲主要讲的是如何在命令行中把命令组合玩出花来用于处理一些奇怪的数据

# 先说结论

```shell
> cat ssh.log | sed -E 's/^.*Disconnected from (invalid |authenticating )?user (.*)[0-9.]+ port [0-9]+( \[preauth\])?$/\2/' | sort | uniq -c | awk '$1 != 1 { print $1 }'| paste -sd+ | bc

> 191665
```

虽然这个东西看起来很唬人, 但是实际上一点也不简单

我们以管道为分隔符逐个拆解一下: 

# cat
这一看就是直接输出ssh日志的内容, 秒了

# sed
一种基于流使用的编辑器

常用的使用方式如下: 
```shell
sed [-e <script>] [文件(支持管道输入)]
```

`-e`是一种动作(可以不写), 后面的script里面以字符串表示对应的指令: 
- `a/i`: 都是插入文字,  前面接位置,后面接字符串, 将字符串插入位置后/前的一行; 如果要插入多行内容用`\n`或者`\`分隔

    `cat <file> | sed '<行号> a/i <string>'`
- `c`: 取代, 后面可以接字符串, 这些字串可以取代指定起始行和结束行中间的内容
  
    `cat <file> | sed '<起始行>,<结束行> c <string>'`
- `d`: 删除, 可以指定起始行和结束行, 其中`$`表示最后一行; 或者只指定一行也可以

    `cat <file> | sed '<起始行>,<结束行> d'`

    `cat <file> | sed '<起始行> d'`

- `p`: 打印, 通常会与参数 `-n` 一起用, 否则会先输出所有文件内容

    `cat <file> | sed [-n] '<起始行>,<结束行> p'`
- `s`: 替换, 支持正则表达式
  
    `cat <file> | sed [-E(有些正则表达式需要这个参数)] 's/<old>/<new>/[g(表示全局操作, 否则只执行第一个)]'`
- `/<string>/<exec>`: 匹配一串内容, 支持正则, 后面要跟一些操作, 也可以是多个操作的集合
  
    `cat <file> | sed -n '/oo/{s/oo/kk/;p;q}`: 先找匹配内容, 然后打印, `q`表示仅执行一次

记得将要做的操作用两个单引号装起来
注意到`sed`可以直接修改文件内容, 这功能非常有帮助(比如打开一些编辑器打不开的大文件但只要修改一些小地方)

那么, `sed -E 's/^.*Disconnected from (invalid |authenticating )?user (.*)[0-9.]+ port [0-9]+( \[preauth\])?$/\2/'`即为: 
- 先匹配`^.*Disconnected from (invalid |authenticating )?user (.*)[0-9.]+ port [0-9]+( \[preauth\])?$`
- 这段内容替换为将第二个匹配组(即用户名)

# sort 
就是排序, 默认输出为由小到大

使用方法: 
```shell
sort [OPTIONS] [<文件(支持管道输入)>]
```

| 常用参数 | 说明 |
| --- | --- |
| -f | 排序时, 将小写字母视为大写字母 |
| -M | 将前面3个字母依照月份的缩写进行排序 |
| -n | 依照数值的大小排序 |
| -o <输出文件> | 将结果存入文件 |
| -r | 反转排序顺序 |
| [-k field1[,field2]] | 按指定的列作为优先级排序(可以指定两个) |

# uniq
与vector的unique类似, 针对已经排序好的东西使用

使用方法: 
```shell
uniq [OPTIONS] [<文件(支持管道输入)>] [<输出文件>]
```
| 常用参数 | 说明 |
| --- | --- |
| -c | 在每列旁边显示该行重复出现的次数 |
| -d | 仅显示重复出现的行列 |
| -u | 与-d相对, 仅显示出一次的行列 |

那么, `sort | uniq -c`即可实现将所有的用户名去重, 并在第一列显示每个用户名出现的次数

如果想选出用户个数, 可以使用`wc -l`直接统计行数

# 番外:  wc
用于统计文件的一些数据

使用方法: 
```shell
`wc [-clw] [<多个文件(支持管道输入)>]`
```
| 常用参数 | 说明 |
| --- | --- |
| -c | 只显示Bytes数 |
| -l | 显示行数 |
| -w | 只显示字数 |

若不指定参数, 公式化输出为: 
```shell
<行数> <单词数> <字节数> <第一个文件>
...
<行数> <单词数> <字节数> total
```

# awk
同为文本编辑器, 在列操作上提供了更多支持

一个相对有点关系的命令是`nl <file>`, 用途是在打印的内容的每一行前面加上行号

使用方法: 
```shell
awk [OPTIONS] '<pattern> {<action>}' [<文件(支持管道输入)>]
```
参数常用的只有`-f <脚本文件>`, 用于直接加载一个已经写好的awk脚本; 和`-F<分隔符>`, 用于指定列之间的分隔符

`<pattern> {<action>}`对应awk自身的编程语言, 列举出来比较复杂, 这里就不讲了(逃)

关于`awk`、`sed`、`grep`的区分: 
- `grep` 更适合单纯的查找或匹配文本
- `sed` 更适合编辑匹配到的文本
- `awk` 更适合格式化文本, 对文本进行较复杂格式处理

在示例中, `awk '$1 != 1 { print $1 }'`表示如果第一列的值不为1就打印

# paste
将输入的内容按**列**合并(按行合并可以用cat)

使用方法: 
```shell
paste [-s] [-d <间隔字符>] [<文件(支持管道输入)>]
```
注意`paste`默认仍为按行, `-s`用于指定按列
`-d`可以修改合并时的间隔字符, 默认为空格

那么, `paste -sd+`有点像`"+".join()`, 也就是把列数据以加号相连接, 得到了一个巨大加法表达式

但众所周知字符串在命令行内是不能直接计算的, 在python中固然有`eval()`, 而在命令行中我们请出的是: 

# bc
全名为berkeley calculator(大草), 我们命令行也要有自己的计算器

使用方法: 
```shell
bc <[特殊变量赋值];字符串表达式(支持管道输入)>
```

表达式语法基本和c++一致

有三个特殊变量: `scale` 来设定小数位, `ibase` 和 `obase` 来设定输入进制和输出进制

# 加在一起

全部加起来~

从ssh日志中筛选连接失败的重复登录尝试的用户名, 并加起来统计总和

你学废了吗?