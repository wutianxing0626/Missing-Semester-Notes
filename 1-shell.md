# 一些bash的cheating paper

在bash中, 参数用空格分割, 如果想用空格作为参数本身的一部分需要`写在字符串内`或者`转义`或者`用单引号强行包裹`

`$PATH` 可以查看环境变量, `which program`查看`program`指令的程序的绝对路径

`cd`: `~`总会导航到自己的主目录, `-`会回到上一个待的目录(不断`cd -`可以在两个目录之间来回跳跃)

`mv/cp <旧的文件路径> <新的文件路径>`

^L 或者`clear`都可以做到清除终端所有内容并回到顶部

`man <program>` 一般可以拿到一个程序的使用说明

`xdg-open <file_name>`: 等效于双击(原生linux好像没有, 需要`apt install xdg-utils`)(windows对应命令为`start`)

`shebang`: 在程序第一行使用, 用于指定执行文件, 例如`#!/bin/bash`


# 详解ls

最简单的用法:  ls 后可以加路径名直接列出该路径下的内容

先对着man ls 捣鼓一下: 

```shell
ls [OPTION]... [FILE]...
```

| 常用参数 | 作用 |
| --- | --- |
| -a | 列出目录下的所有文件, 包括隐藏文件(一般以.开头)|
| -A | 同-a, 但不列出"."和".." |
| -i | 印出每个文件的inode号.  |
| -R | 递归地列出当前目录及其子目录中的文件和目录.  |
| -s | 在每个文件的后面打印出文件的大小.  |
| -h | 以容易理解的格式列出文件大小(例如1K、234M、2G).  |
| -l | 除了文件名之外, 还将文件的权限、所有者、大小、修改日期、软连接/硬链接等信息详细列出来.  |
| -g | 类似-l, 但不列出所有者.  |
| -o | 类似-l, 但不列出组的信息.  |

ls 命令的FILE参数可以使用通配符进行模式匹配, 例如 * 表示匹配任意字符, ? 表示匹配一个字符, [...] 表示匹配指定范围内的字符

例如: 
```shell
ls *.txt         # 列出所有扩展名为.txt的文件
ls file?.txt     # 列出文件名为file?.txt的文件, 其中?表示任意一个字符
ls [abc]*.txt    # 列出以a、b或c开头、扩展名为.txt的文件
```

(注意: 通配符和正则表达式并不同, 如果想用正则表达式筛选可以将输出后的结果导入`grep`)

当你使用`ls -aisl`后可以看到这么个东西: 
```shell
total 98792
 1424    24 drwxr-xr-x  2 root root       24576 Jan  7 00:58 .
65537     4 drwxr-xr-x 14 root root        4096 Sep 28 05:35 ..
 1425     0 lrwxrwxrwx  1 root root           4 Feb 17  2020 NF -> col1
 ...
```

当然这里东西有点多, 因为列出了对应的inode号并且重复了两次文件大小

一般来说如果想详尽的掌握一个文件夹内的信息, `ls -Ahl`足矣, 公式化输出大概长这样: 

```shell
total <文件夹总大小, 以-h的大小形式表示>

<一个十位字符串> <refcnt> <拥有者> <所属群组> <-h的文件大小> <最后修改时间> <处理后的文件名>
```

下面具体解析一下某些列的含义: 
- 十位字符串: 
  - 第1位: 文件类型, d为目录, -为普通文件, l为链接
  - 第2-4位, 5-7位, 6-9位: 每三位一组表明了某个身份拥有的权限, 身份的顺序为: owner、group、others, 权限的顺序为: readable、writable、executable
  
  例如: "-r-xr-x---"的含义为:
  - 当前文档是一个文件
  - 拥有者权限: 可读、不可编辑、可执行
  - 同一个群组下的用户权限: 可读、可执行
  - 其他人: 没有任何权限
- 拥有者, 所属群组: 
  - 怎么区分owner, group, others: 创建文件时owner默认为创建的用户, group默认为用户所在的群组, others默认为group对于所有user的补集
  - `chown <用户名(:群组名)> <文件名>` 可以修改文件对应的用户(和群组)
  - `chgrp` 可以单独修改群组

- 处理后的文件名: 一般来说就是显示目录和文件的名字, 对链接会以`链接名->指向的东西`显示

# 什么是目录的权限? 怎么修改文件的权限?

对于目录来说, rwx的意义和文件并不相同: 
- r: 你是否允许看到这个目录里面的文件, 等效于阅读这个目录文件本身存储的信息, ls指令会受影响
- w: 你是否允许在这个目录内重命名, 创建或删除文件, 等效于修改目录文件和相关的链接
- x: 你是否允许直接进入该目录(的文件干事), 等效于(这个我也想不出来啊喂). 例如, 想修改某个目录下的文件需要对该文件绝对路径上所有目录均有执行权限

修改文件权限: 一般用`chmod`命令, 可以用两种方式调整权限
- 符号模式: `chmod <(身份)(希望做的操作)(对应的权限) 如果对多个身份同时改动的话中间用逗号分隔> <文件名>`可以针对某个文件修改权限
  - 身份: `u`为user, `g`为group, `o`为others
  - 希望做的操作: `+`为增加某个权限, `-`为去除某个权限, `=`为设置为某个权限
  - 对应的权限: rwx组合即可
  
  例如: `chmod u+rwx,g-rx,o=r file.txt`的含义为:
  - 为拥有者增加所有权限: 可读、可编辑、可执行
  - 同一个群组下的用户去除可读、可执行权限
  - 其他人权限改为可读
- 数字模式: 注意到rwx其实对应一个三位二进制数, 读权限为4, 写权限为2, 执行权限为1. 因此可以将这些数字相加, 用0-7来设置对应的权限. 例如常用的`chmod 777 file.txt`就是在任意条件下都拥有了对这个文件的所有权限

# 重定向与管道机制(哇, 还有IPC)

`<` 与 `>` 分别是输入/输出重定向(一般都是用文件)

`>` 是write,  而`>>`是append

`|` 管道机制: 左边输出作为右边输入

`head/tail -n<number>`: 将输入内容的头/尾number行重新输出

`grep <一种模式>`:  将输入内容符合模式的行重新输出, 支持正则表达式; 或者 `grep <一种模式> <你要搜索的东西>`

`tee <file_name>`:  将输入内容同时输出到屏幕和写入文件中

# 课堂练习

## 为什么 cd 必须是 shell 内建命令, 而不能是独立程序?

*cd 要修改的是"当前 shell 进程自己"的工作目录, 而独立程序没有能力修改父进程的状态*

执行外部命令时, shell会fork出一个子进程再exec加载程序, 子进程从父进程继承一份当前工作目录的拷贝而不是共享

同理, 需要修改 shell 自身状态的命令都是内建:
- `export`/`unset`: 要改当前 shell 的环境变量
- `source`/`.`: 要在当前进程执行, 这样脚本设置的变量和 cd 才对 shell 生效
- `exit`: 要终止当前 shell 进程本身
- `jobs`/`fg`/`bg`: 要操作当前 shell 管理的子进程
